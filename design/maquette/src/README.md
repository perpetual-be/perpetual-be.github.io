# design/maquette/src — outils de la maquette (lot 2)

Exécuter depuis la racine du dépôt.

- `build.mjs` → génère les pages de `design/maquette/` (`index.html`, puis la fiche et la vue Projets) à partir de `data/*.json`, `content/*.md`, `public/favicon.svg`, `public/partners/encre/*.svg` et `carte/belgique.svg`. Aucune dépendance hors Node : `node design/maquette/src/build.mjs`.
- `carte/belgique.svg` → la carte de la section Réalisations (contour, 17 points, 17 étiquettes placées à la main ; les quatre adresses bruxelloises sont un seul point plus gros, r 6,5, sous l'étiquette « Bruxelles » ; `belgique.json` porte les coordonnées et le groupe). `build.mjs` l'inline en regroupant chaque étiquette avec son point (`<g class="carte__lieu">`) : le survol d'un point colore l'étiquette en CSS seul (les villes sont figées sur « toutes », visibles sans survol). `data-rang="1"` sur une étiquette du SVG la garde visible sur mobile (les autres s'y effacent, faute de place) ; pour changer la sélection, ajouter ou retirer l'attribut dans le SVG, puis rebâtir.
- `check.mjs` → vérifications automatiques (premier écran à 1440 × 900 et 1366 × 703 dans les deux structures, débordement horizontal, effet de chaque bascule, dosage figé et alignement, en-tête, carte et bande des réalisations, logos partenaires, photo 2800 px, mode présentation, page sans JavaScript, polices, contrastes).
- `export.mjs` → captures PNG, panneau masqué ; par défaut les pages présentes en 1440 pleine page et 390, échelle 2, dans `design/maquette/export/`. Options : `--etat "ecran=photo&serif=source-serif"`, `--pages`, `--formats 1440,1366,390`, `--out`, `--echelle`, `--ecran`.

`check.mjs` et `export.mjs` demandent Playwright : `npm install -D playwright` à la racine (puis `npx playwright install chromium`), ou une installation globale avec `NODE_PATH=$(npm root -g) node design/maquette/src/export.mjs`.

## Figé le 22/09 (écrit dans `:root` de `maquette.css`, plus dans le panneau)

- Sans = Instrument Sans ; wordmark = Jost 400, capitales, interlettrage 0,18 em (le tracé PNG et son masque ne sont plus dans la page ; la règle vaut à toutes les largeurs, et sous 641 px l'en-tête ne montre que le Φ) ; Φ = bronze `#684E1E` ; section projets = liste des 4.
- Le dosage, à la place des anciennes bascules 7 et 8 : bande papier `#F9F9F6` ; chiffres clés or `#9A7A3B` ; élément décoratif de la bande or `#C9B58A` (deux arcs par défaut) ; graphique, barres anthracite `#26231F` sur piste sable `#F6F1E6` (`--surface-piste` est détachée de `--surface`) ; points de la carte anthracite ; fond du pays sur `--surface-carte`, détachée de `--surface` : sable `#F6F1E6`, pour se lire malgré ses bords irréguliers ; étiquettes des chiffres gris chaud `#7A7466` (`--c-etiquette-chiffre`) ; pied de page sur `--surface-pied`, blanc par défaut ; lien « Tous les projets → » or foncé `#8A6B2F` ; mail du pied de page anthracite, souligné (`--c-mail`, distinct de `--c-lien`) ; Φ bronze, trait de survol or.
- Alignement : `--hero-haut` 112 px (84 px sous 720 px de haut) ; en-tête pleine largeur, `padding: 0 var(--gutter)`, le contenu texte reste sur 1200 px.
- Retours du 22/09 : carte détachée en sable et portée à 700 px (grille 8/4, texte aligné en haut, étiquettes à 11 px, décalages augmentés dans la grappe centrale) ; structure F (`?ecran=photo-bande`) ajoutée à la bascule 9 ; élément « 4 · Φ en filigrane » retiré de la bascule 10, défaut sur les arcs ; nouvelles bascules 15c (contour de la carte pleine), 16a (position de la signature), 18 (fond du pied de page), 19 (étiquettes des chiffres) ; étiquettes des chiffres plafonnées à 22ch (au lieu de 18ch).
- Retours du 22/09 soir : en structure F, hauteur de la photo réduite à `clamp(320px, calc(100vh − 270px), 560px)` (la bande et ses quatre chiffres tiennent maintenant à 1366 × 703 et 1440 × 900 ; structure E inchangée) ; bascule 9 portée à **photo (structure E) en défaut** — colonnes et photo-bande restent au choix, sans JavaScript la page reste la structure E (la base CSS de `.hero` est désormais celle de la structure E, colonnes et structure F sont les bascules) ; carte détachée à 600 px (au lieu de 700) et son bloc de texte centré verticalement (il était calé en haut) ; bascule 15a portée à quatre valeurs (sable défaut / pierre, nouveau jeton `--pierre` `#EBE9E1` / papier-contour / trait), 15c absorbée dedans ; villes figées sur « toutes », bascule 15b retirée (le test de chevauchement de `check.mjs` reste) ; étiquettes des chiffres figées sur gris chaud, bascule 19 retirée ; sous-bascule 16a (position de la signature) retirée, c'était un malentendu — l'accroche et la signature restent à leur place par défaut, la bascule 16 (signature or / anthracite) reste.

## Bascules restantes (défaut en premier)

2 serif Newsreader / Source Serif 4 · 3 portée de la serif rationnée / partout · 17 graisse de la serif 400 / 500 sur l'accroche et les chiffres (les deux serifs portent l'axe `wght`, vérifié dans leur table `fvar`) · 9 premier écran photo — structure E (défaut) / colonnes / photo-bande — structure F, 9b photo Community 05 / Data Box 02 / The Bank 01 / The Bank 03 · 10 élément de la bande deux arcs / anneaux / rien · 16 signature or / anthracite · 15 réalisations carte / bande / aucune, 15a carte sable / pierre / papier-contour / trait · 14 logos partenaires couleur / noir / gris chaud · 18 fond du pied de page blanc / papier / sable.

Premier écran, structure E (défaut, sans attribut `data-ecran`) : en-tête → photo pleine largeur (hauteur `clamp(420px, 100vh − 200px, 620px)`, `object-fit: cover`, voile en dégradé depuis le haut, srcset 1800 / 2800 px) qui porte l'accroche en blanc, en haut à gauche du container → paragraphes en deux colonnes et signature sur blanc → bande des quatre chiffres avec l'élément décoratif → graphique. Structure colonnes (`?ecran=colonnes`) : en-tête → accroche à gauche avec la signature juste dessous, paragraphes à droite → bande des quatre chiffres → graphique. Structure F (`?ecran=photo-bande`) : comme E, mais photo plus basse (`clamp(320px, 100vh − 270px, 560px)`) et bande juste après elle, avant les paragraphes. Téléphone (E et F) : photo de 420 px, accroche en bas à gauche, voile depuis le bas. Les quatre chiffres restent dans la bande dans les trois structures.

Photos : la bande des réalisations lit `design/directions/img/<id>-01-s.jpg` (800 px) pour chaque entrée `agency` et `gallery` de `data/projects.json` ; la photo de la structure E lit `<nom>.jpg` (1800 px) et `<nom>-l.jpg` (2800 px), avec repli sur `<nom>-s.jpg` tant qu'ils manquent (`build.mjs` le signale). Les versions se génèrent depuis les originaux avec `design/directions/src/reduire-photos.mjs --petit | --grand | --tres-grand`.

Les pages s'ouvrent directement dans le navigateur (`file://`). L'état des bascules est dans la query de l'URL (`index.html?ecran=photo&serif=source-serif`), le hash reste aux ancres ; `?panneau=off` masque le panneau, Échap le rappelle. `maquette.css` porte les jetons et les composants, puis une région « bascules » en fin de fichier ; `maquette.js` ne contient que le panneau, la lecture et l'écriture de l'état, et l'aperçu de la liste des projets. `fonts/` ne contient que les polices encore utilisées (Newsreader, Source Serif 4, Instrument Sans, Jost) ; les autres restent dans `design/directions/fonts/`.
